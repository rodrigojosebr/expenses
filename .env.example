# Variáveis de ambiente para autenticação da aplicação
# Mapeia chaves de API para IDs de usuário.
# Crie suas próprias chaves seguras (ex: UUIDs ou senhas fortes).
USER_KEYS_JSON='{
  "sua-api-key-secreta-1": { "id": "user_1a2b3c", "name": "raj" },
  "sua-api-key-secreta-2": { "id": "user_4d5e6f", "name": "roseane" }
}'

# Variáveis de ambiente para conexão com o Vercel KV (para desenvolvimento local)
# Estas variáveis são injetadas automaticamente no ambiente da Vercel.
# Para rodar localmente, conecte seu projeto à Vercel e execute `vercel env pull .env.local`
# Descomente as linhas abaixo se for preenchê-las manualmente.
# KV_URL=
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_REST_API_READ_ONLY_TOKEN=
